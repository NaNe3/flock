import Icon from 'react-native-vector-icons/FontAwesome6'

export default function FAIcon({ name, size, color, style }) {
  return (
    <Icon name={name} size={size} color={color} style={style} />
  )
} 